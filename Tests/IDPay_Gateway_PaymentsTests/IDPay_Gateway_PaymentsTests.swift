import XCTest
@testable import IDPay_Gateway_Payments

final class IDPay_Gateway_PaymentsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(IDPay_Gateway_Payments().text, "Hello, World!")
    }
}
